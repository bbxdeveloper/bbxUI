import { RemoveWhitespacesPipe } from './remove-whitespaces.pipe';

describe('RemoveWhitespacesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveWhitespacesPipe();
    expect(pipe).toBeTruthy();
  });
});
